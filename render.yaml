services:
  - type: web
    name: legal-team-openai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run legal_team_openai.py
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
